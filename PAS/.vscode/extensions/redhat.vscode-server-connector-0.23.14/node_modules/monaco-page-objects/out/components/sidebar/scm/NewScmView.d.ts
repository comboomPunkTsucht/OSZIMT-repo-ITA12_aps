import { ScmView, ScmProvider, ScmChange } from "./ScmView";
import { ContextMenu } from "../../menu/ContextMenu";
/**
 * New SCM view for code 1.47 onwards
 */
export declare class NewScmView extends ScmView {
    getProviders(): Promise<ScmProvider[]>;
}
/**
 * Implementation for a single SCM provider
 */
export declare class SingleScmProvider extends ScmProvider {
    /**
     * There is no title available for a single provider
     */
    getTitle(): Promise<string>;
    /**
     * No title available for single provider
     */
    getType(): Promise<string>;
    takeAction(title: string): Promise<boolean>;
    openMoreActions(): Promise<ContextMenu>;
    getChanges(staged?: boolean): Promise<ScmChange[]>;
}
/**
 * Implementation of an SCM provider when multiple providers are available
 */
export declare class MultiScmProvider extends ScmProvider {
    takeAction(title: string): Promise<boolean>;
    openMoreActions(): Promise<ContextMenu>;
    commitChanges(message: string): Promise<void>;
    getChanges(staged?: boolean): Promise<ScmChange[]>;
    getChangeCount(staged?: boolean): Promise<number>;
}
