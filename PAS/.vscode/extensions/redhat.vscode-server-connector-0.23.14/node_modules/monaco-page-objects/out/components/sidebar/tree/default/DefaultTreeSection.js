"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DefaultTreeSection = void 0;
const TreeSection_1 = require("../TreeSection");
const selenium_webdriver_1 = require("selenium-webdriver");
const DefaultTreeItem_1 = require("./DefaultTreeItem");
/**
 * Default view section
 */
class DefaultTreeSection extends TreeSection_1.TreeSection {
    getVisibleItems() {
        return __awaiter(this, void 0, void 0, function* () {
            const items = [];
            const elements = yield this.findElements(DefaultTreeSection.locators.DefaultTreeSection.itemRow);
            for (const element of elements) {
                items.push(yield new DefaultTreeItem_1.DefaultTreeItem(element, this).wait());
            }
            return items;
        });
    }
    findItem(label, maxLevel = 0) {
        return __awaiter(this, void 0, void 0, function* () {
            yield this.expand();
            const container = yield this.findElement(DefaultTreeSection.locators.DefaultTreeSection.rowContainer);
            yield container.sendKeys(selenium_webdriver_1.Key.HOME);
            let item = undefined;
            do {
                const temp = yield container.findElements(DefaultTreeSection.locators.DefaultTreeItem.ctor(label));
                if (temp.length > 0) {
                    const level = +(yield temp[0].getAttribute(DefaultTreeSection.locators.ViewSection.level));
                    if (maxLevel < 1 || level <= maxLevel) {
                        item = yield new DefaultTreeItem_1.DefaultTreeItem(temp[0], this).wait();
                    }
                }
                else {
                    const lastrow = yield container.findElements(DefaultTreeSection.locators.DefaultTreeSection.lastRow);
                    if (lastrow.length > 0) {
                        break;
                    }
                    yield container.sendKeys(selenium_webdriver_1.Key.PAGE_DOWN);
                }
            } while (!item);
            return item;
        });
    }
}
exports.DefaultTreeSection = DefaultTreeSection;
//# sourceMappingURL=DefaultTreeSection.js.map